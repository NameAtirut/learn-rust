//! `learn-rust` — see `examples/` for the curriculum and `README.md` for the
//! schedule. This library crate is intentionally empty; all material lives
//! in runnable examples so each lesson can be invoked with
//! `cargo run --example hN_topic`.
